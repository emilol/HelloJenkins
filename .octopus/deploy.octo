schema_version = 1.0
process {
  inline_script_action "Hello world" {
    run_on_server = true
    syntax = "PowerShell"
    body = <<SCRIPT
      echo "Hello world"
    SCRIPT
    environments = []
  }
}
